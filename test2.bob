def main(; vet, t, i, j) {
    t = 0;
    print("Digite o tamanho do vetor: ");
    scanf("int", t);
    vet[t];
    for(i = 0; i < t; i++) {
        print("Digite o valor da posição " + i);
        scanf("int", vet[i]);
    }

    foreach j in vet {
        print(j);
    }
}