def Baskara::calculaRaizes(; delta, raiz1, raiz2)
{
     delta = b*b - 4*a*c;
     if (delta < 0) {
        print("nao tem raiz real\n");
     } else {
        raiz1 = -b + delta / (2*a);
        raiz2 = -b - delta / (2*a);
        print("raiz1 = \"", raiz1, "\"\n");
        print("raiz2 = \"", raiz2, "\"\n");
     }
}

