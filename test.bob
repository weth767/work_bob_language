class Baskara {
    static var instanciasCriadas;
    var a, b, c;
    static def inicializa();
    def Baskara(a, b, c);
    def calculaRaizes();
}

def Baskara::inicializa() {
    instanciasCriadas = 0;
}

def main (; a, b, c, d) {
    Baskara->inicializa();
    #b = new Baskara();
}